    1: import asyncio
    1: import sys
    1: import types
    1: import unittest
    1: from types import SimpleNamespace
    1: from unittest.mock import patch
       
    1: if "psycopg" not in sys.modules:
           psycopg = types.ModuleType("psycopg")
           psycopg_rows = types.ModuleType("psycopg.rows")
           psycopg_rows.dict_row = object()
           psycopg.rows = psycopg_rows
           sys.modules["psycopg"] = psycopg
           sys.modules["psycopg.rows"] = psycopg_rows
       
    1: if "psycopg_pool" not in sys.modules:
           psycopg_pool = types.ModuleType("psycopg_pool")
       
           class ConnectionPool:  # pragma: no cover - stub for unit tests
               def __init__(self, *args, **kwargs):
                   self.args = args
                   self.kwargs = kwargs
       
               def close(self):
                   return None
       
           psycopg_pool.ConnectionPool = ConnectionPool
           sys.modules["psycopg_pool"] = psycopg_pool
       
    1: from app.routers.invites import accept_invite
    1: from app.routers.projects import ProjectCreate, create_project
    1: from app.routers.sprints import sprint_schedule
    1: from app.routers.tenants import TenantCreate, create_tenant
    1: from app.routers.workspaces import board_defaults as workspace_board_defaults
    1: from app.routers.workspaces import schedule as workspace_schedule
    1: from app.routers.tenants import my_tenants
    1: from app.routers.users import ActiveTenantRequest, set_active_tenant
    1: from app.security import get_current_user
    1: from app.services.memberships import active_membership_for_user
    1: from app.services.workspace_defaults import ensure_default_project, ensure_workspace_board_defaults
       
       
    2: class FakeCursor:
    1:     def __init__(self, resolver):
    5:         self.resolver = resolver
    5:         self.executed = []
    5:         self.current = None
       
    1:     def __enter__(self):
    5:         return self
       
    1:     def __exit__(self, exc_type, exc, tb):
    5:         return False
       
    1:     def execute(self, query, params=()):
    8:         self.executed.append((query, params))
    8:         self.current = self.resolver(query, params)
       
    1:     def fetchone(self):
    7:         return self.current
       
       
    2: class FakeConn:
    1:     def __init__(self, resolver):
    5:         self.cursor_obj = FakeCursor(resolver)
       
    1:     def cursor(self):
    5:         return self.cursor_obj
       
       
    2: class FakeConnManager:
    1:     def __init__(self, resolver):
    5:         self.conn = FakeConn(resolver)
       
    1:     def __enter__(self):
    5:         return self.conn
       
    1:     def __exit__(self, exc_type, exc, tb):
    5:         return False
       
       
    2: class WorkspaceBootstrapTests(unittest.TestCase):
    1:     def test_active_membership_prefers_active_tenant(self):
    1:         memberships = [
    1:             {"tenant_id": "t1", "role": "MEMBER", "tenant_name": "One", "tenant_slug": "one"},
    1:             {"tenant_id": "t2", "role": "OWNER", "tenant_name": "Two", "tenant_slug": "two"},
               ]
    1:         active = active_membership_for_user("user-1", "t2", memberships)
    1:         self.assertEqual(active["tenant_id"], "t2")
    1:         self.assertEqual(active["role"], "OWNER")
       
    1:     def test_ensure_default_project_returns_none_without_tenant(self):
    1:         self.assertIsNone(ensure_default_project(""))
       
    2:     @patch("app.services.workspace_defaults.get_conn")
    2:     @patch("app.services.workspace_defaults.fetch_one")
    2:     def test_ensure_default_project_reuses_existing_row(self, mock_fetch_one, mock_get_conn):
    1:         mock_fetch_one.side_effect = [
    1:             {"id": "tenant-1", "name": "Grabbit"},
    1:             {"id": "project-1", "tenant_id": "tenant-1", "key": "GRABBIT", "name": "Grabbit"},
               ]
    1:         project = ensure_default_project("tenant-1")
    1:         self.assertEqual(project["id"], "project-1")
    1:         mock_get_conn.assert_not_called()
       
    2:     @patch("app.security.memberships_for_user")
    2:     @patch("app.security.fetch_one")
    2:     @patch("app.security.decode_token")
    2:     def test_get_current_user_resolves_single_membership(self, mock_decode, mock_fetch_one, mock_memberships_for_user):
    1:         mock_decode.return_value = {"sub": "user-1"}
    1:         mock_fetch_one.side_effect = [
    1:             {"id": "user-1", "name": "Ada", "email": "ada@example.com", "active_tenant_id": None, "tenant_id": None, "role": None, "tenant_name": None, "tenant_slug": None},
    1:             {"id": "user-1", "name": "Ada", "email": "ada@example.com", "active_tenant_id": None, "tenant_id": None, "role": None, "tenant_name": None, "tenant_slug": None},
               ]
    1:         mock_memberships_for_user.return_value = [
    1:             {"tenant_id": "tenant-1", "role": "OWNER", "tenant_name": "Grabbit", "tenant_slug": "grabbit"}
               ]
    1:         request = SimpleNamespace(query_params={"token": "test-token"})
    1:         current_user = get_current_user(request, credentials=None)
    1:         self.assertEqual(current_user["tenant_id"], "tenant-1")
    1:         self.assertEqual(current_user["role"], "OWNER")
    1:         self.assertEqual(current_user["tenant_name"], "Grabbit")
       
    2:     @patch("app.security.memberships_for_user")
    2:     @patch("app.security.fetch_one")
    2:     @patch("app.security.decode_token")
    2:     def test_get_current_user_uses_active_tenant_without_membership_lookup(self, mock_decode, mock_fetch_one, mock_memberships_for_user):
    1:         mock_decode.return_value = {"sub": "user-1", "tenant_id": "tenant-1"}
    1:         mock_fetch_one.return_value = {"id": "user-1", "name": "Ada", "email": "ada@example.com", "active_tenant_id": "tenant-1", "tenant_id": "tenant-1", "role": "OWNER", "tenant_name": "Grabbit", "tenant_slug": "grabbit"}
    1:         request = SimpleNamespace(query_params={"token": "test-token"})
    1:         current_user = get_current_user(request, credentials=None)
    1:         self.assertEqual(current_user["tenant_id"], "tenant-1")
    1:         self.assertEqual(current_user["role"], "OWNER")
    1:         mock_memberships_for_user.assert_not_called()
       
    2:     @patch("app.security.memberships_for_user")
    2:     @patch("app.security.fetch_one")
    2:     @patch("app.security.decode_token")
    2:     def test_get_current_user_with_multiple_memberships_skips_extra_lookup(self, mock_decode, mock_fetch_one, mock_memberships_for_user):
    1:         mock_decode.return_value = {"sub": "user-1"}
    1:         mock_fetch_one.return_value = {"id": "user-1", "name": "Ada", "email": "ada@example.com", "active_tenant_id": None, "tenant_id": None, "role": None, "tenant_name": None, "tenant_slug": None}
    1:         mock_memberships_for_user.return_value = [
    1:             {"tenant_id": "tenant-1", "role": "OWNER", "tenant_name": "Grabbit", "tenant_slug": "grabbit"},
    1:             {"tenant_id": "tenant-2", "role": "MEMBER", "tenant_name": "Orbit", "tenant_slug": "orbit"},
               ]
    1:         request = SimpleNamespace(query_params={"token": "test-token"})
    1:         current_user = get_current_user(request, credentials=None)
    1:         self.assertEqual(mock_fetch_one.call_count, 1)
    1:         self.assertIsNone(current_user["tenant_id"])
    1:         self.assertIsNone(current_user["role"])
       
    2:     @patch("app.routers.tenants.invite_url_for_tenant", return_value="/invite/invite-1")
    2:     @patch("app.routers.tenants.create_token", return_value="token-1")
    2:     @patch("app.routers.tenants.ensure_workspace_invite")
    2:     @patch("app.routers.tenants.get_conn")
    2:     @patch("app.routers.tenants.fetch_one")
    2:     def test_create_tenant_uses_inserted_tenant_id_for_owner_membership(
               self,
               mock_fetch_one,
               mock_get_conn,
               mock_ensure_workspace_invite,
               mock_create_token,
               mock_invite_url_for_tenant,
           ):
    1:         mock_fetch_one.return_value = None
    1:         tenant_row = {"id": "tenant-1", "name": "Grabbit", "slug": "grabbit", "invite_code": "invite-1", "invite_enabled": True}
    1:         membership_row = {"tenant_id": "tenant-1", "user_id": "user-1", "role": "OWNER", "status": "ACTIVE", "joined_at": "2026-06-23T00:00:00+00:00"}
    1:         executed = []
       
    1:         def resolver(query, params):
    3:             normalized = " ".join(query.split()).lower()
    3:             executed.append((normalized, params))
    3:             if normalized.startswith("insert into tenants"):
    1:                 return tenant_row
    2:             if normalized.startswith("insert into tenant_members"):
    1:                 self.assertEqual(params[0], tenant_row["id"])
    1:                 return membership_row
    1:             if normalized.startswith("update users set active_tenant_id"):
    1:                 self.assertEqual(params[0], tenant_row["id"])
    1:                 self.assertEqual(params[1], "user-1")
    1:                 return None
                   return None
       
    1:         mock_get_conn.return_value = FakeConnManager(resolver)
    1:         mock_ensure_workspace_invite.return_value = tenant_row
       
    1:         response = create_tenant(TenantCreate(name="Grabbit"), current_user={"id": "user-1"})
       
    1:         self.assertEqual(response["tenant"]["id"], "tenant-1")
    1:         self.assertEqual(response["membership"]["role"], "OWNER")
    1:         self.assertEqual(response["access_token"], "token-1")
    1:         self.assertEqual(response["invite_url"], "/invite/invite-1")
    1:         mock_ensure_workspace_invite.assert_called_once_with("tenant-1")
    2:         self.assertTrue(any("insert into tenants" in query for query, _ in executed))
    4:         self.assertTrue(any("insert into tenant_members" in query for query, _ in executed))
       
    3:     @patch("app.routers.invites.memberships_for_user", return_value=[
    1:         {"tenant_id": "tenant-1", "role": "MEMBER", "tenant_name": "Grabbit", "tenant_slug": "grabbit"}
           ])
    2:     @patch("app.routers.invites.create_token", return_value="token-accept")
    2:     @patch("app.routers.invites.get_conn")
    2:     @patch("app.routers.invites.execute")
    2:     @patch("app.routers.invites.fetch_one")
    2:     def test_accept_invite_marks_new_member_as_not_already_member(
               self,
               mock_fetch_one,
               mock_execute,
               mock_get_conn,
               mock_create_token,
               mock_memberships_for_user,
           ):
    1:         tenant_row = {"id": "tenant-1", "name": "Grabbit", "slug": "grabbit", "invite_code": "invite-1", "invite_enabled": True}
    1:         membership_row = {"tenant_id": "tenant-1", "user_id": "user-1", "role": "MEMBER", "status": "ACTIVE"}
    1:         mock_fetch_one.side_effect = [tenant_row, None]
       
    1:         def resolver(query, params):
    1:             normalized = " ".join(query.split()).lower()
    1:             if normalized.startswith("update users set active_tenant_id"):
    1:                 return {"active_tenant_id": "tenant-1"}
                   return None
       
    1:         mock_execute.return_value = membership_row
    1:         mock_get_conn.return_value = FakeConnManager(resolver)
       
    1:         response = accept_invite("invite-1", current_user={"id": "user-1"})
       
    1:         self.assertFalse(response["already_member"])
    1:         self.assertEqual(response["active_tenant_id"], "tenant-1")
    1:         self.assertEqual(response["membership"]["role"], "MEMBER")
    1:         self.assertEqual(response["access_token"], "token-accept")
    1:         mock_execute.assert_called_once()
    1:         mock_memberships_for_user.assert_called_once_with("user-1")
       
    3:     @patch("app.routers.invites.memberships_for_user", return_value=[
    1:         {"tenant_id": "tenant-1", "role": "OWNER", "tenant_name": "Grabbit", "tenant_slug": "grabbit"}
           ])
    2:     @patch("app.routers.invites.create_token", return_value="token-existing")
    2:     @patch("app.routers.invites.get_conn")
    2:     @patch("app.routers.invites.execute")
    2:     @patch("app.routers.invites.fetch_one")
    2:     def test_accept_invite_existing_member_does_not_reinsert_membership(
               self,
               mock_fetch_one,
               mock_execute,
               mock_get_conn,
               mock_create_token,
               mock_memberships_for_user,
           ):
    1:         tenant_row = {"id": "tenant-1", "name": "Grabbit", "slug": "grabbit", "invite_code": "invite-1", "invite_enabled": True}
    1:         existing_membership = {"tenant_id": "tenant-1", "user_id": "user-1", "role": "OWNER", "status": "ACTIVE"}
    1:         mock_fetch_one.side_effect = [tenant_row, existing_membership]
       
    1:         def resolver(query, params):
    1:             normalized = " ".join(query.split()).lower()
    1:             if normalized.startswith("update users set active_tenant_id"):
    1:                 return {"active_tenant_id": "tenant-1"}
                   return None
       
    1:         mock_get_conn.return_value = FakeConnManager(resolver)
       
    1:         response = accept_invite("invite-1", current_user={"id": "user-1"})
       
    1:         self.assertTrue(response["already_member"])
    1:         self.assertEqual(response["membership"]["role"], "OWNER")
    1:         self.assertEqual(response["access_token"], "token-existing")
    1:         mock_execute.assert_not_called()
    1:         mock_memberships_for_user.assert_called_once_with("user-1")
       
    2:     @patch("app.services.workspace_defaults.ensure_default_project")
    2:     @patch("app.services.workspace_defaults.ensure_current_monthly_sprint")
    2:     def test_workspace_defaults_short_circuit_without_tenant(self, mock_sprint, mock_project):
    1:         result = ensure_workspace_board_defaults("")
    1:         self.assertEqual(result, {"project": None, "sprint": None})
    1:         mock_project.assert_not_called()
    1:         mock_sprint.assert_not_called()
       
    2:     @patch("app.routers.workspaces.ensure_workspace_board_defaults")
    2:     def test_workspace_board_route_returns_defaults_for_active_tenant(self, mock_defaults):
    1:         mock_defaults.return_value = {"project": {"id": "project-1"}, "sprint": {"id": "sprint-1"}}
    1:         result = workspace_board_defaults(current_user={"tenant_id": "tenant-1", "active_tenant_id": None})
    1:         self.assertEqual(result["project"]["id"], "project-1")
    1:         self.assertEqual(result["sprint"]["id"], "sprint-1")
    1:         mock_defaults.assert_called_once_with("tenant-1")
       
    2:     @patch("app.routers.workspaces.get_workspace_sprint_schedule")
    2:     def test_workspace_schedule_route_uses_current_tenant(self, mock_schedule):
    1:         mock_schedule.return_value = {"autoSprintEnabled": True, "frequency": "Monthly"}
    1:         result = workspace_schedule(current_user={"active_tenant_id": "tenant-2", "tenant_id": None})
    1:         self.assertTrue(result["autoSprintEnabled"])
    1:         self.assertEqual(result["frequency"], "Monthly")
    1:         mock_schedule.assert_called_once_with("tenant-2")
       
    2:     @patch("app.routers.sprints.get_workspace_sprint_schedule")
    2:     @patch("app.routers.sprints.ensure_workspace_board_defaults")
    2:     def test_sprint_schedule_route_bootstraps_defaults_once(self, mock_defaults, mock_schedule):
    1:         mock_schedule.return_value = {"autoSprintEnabled": True, "frequency": "Monthly"}
    1:         result = sprint_schedule(current_user={"tenant_id": "tenant-3", "active_tenant_id": None})
    1:         self.assertTrue(result["autoSprintEnabled"])
    1:         mock_defaults.assert_called_once_with("tenant-3")
    1:         mock_schedule.assert_called_once_with("tenant-3", None)
       
    3:     @patch("app.routers.tenants.memberships_for_user", return_value=[
    1:         {"tenant_id": "tenant-1", "role": "OWNER", "tenant_name": "Grabbit", "tenant_slug": "grabbit"},
    1:         {"tenant_id": "tenant-2", "role": "MEMBER", "tenant_name": "Orbit", "tenant_slug": "orbit"},
           ])
    2:     def test_my_tenants_returns_memberships_and_active_tenant(self, mock_memberships_for_user):
    1:         result = my_tenants(current_user={"id": "user-1", "active_tenant_id": "tenant-2"})
    1:         self.assertEqual(result["active_tenant_id"], "tenant-2")
    1:         self.assertEqual(len(result["tenants"]), 2)
    1:         self.assertEqual(result["tenants"][0]["tenant_id"], "tenant-1")
    1:         mock_memberships_for_user.assert_called_once_with("user-1")
       
    3:     @patch("app.routers.users.memberships_for_user", return_value=[
    1:         {"tenant_id": "tenant-1", "role": "OWNER", "tenant_name": "Grabbit", "tenant_slug": "grabbit"}
           ])
    2:     @patch("app.routers.users.get_conn")
    2:     @patch("app.routers.users.fetch_one")
    2:     def test_set_active_tenant_returns_updated_session_and_token(self, mock_fetch_one, mock_get_conn, mock_memberships_for_user):
    1:         mock_fetch_one.return_value = {"role": "OWNER", "id": "tenant-1", "name": "Grabbit", "slug": "grabbit"}
       
    1:         def resolver(query, params):
    1:             normalized = " ".join(query.split()).lower()
    1:             if normalized.startswith("update users set active_tenant_id"):
    1:                 return {"active_tenant_id": "tenant-1"}
                   return None
       
    1:         mock_get_conn.return_value = FakeConnManager(resolver)
       
    1:         response = set_active_tenant(ActiveTenantRequest(tenant_id="tenant-1"), current_user={"id": "user-1", "active_tenant_id": None})
       
    1:         self.assertEqual(response["active_tenant_id"], "tenant-1")
    1:         self.assertEqual(response["membership"]["role"], "OWNER")
    1:         self.assertEqual(response["tenant"]["id"], "tenant-1")
    1:         self.assertEqual(len(response["memberships"]), 1)
    1:         mock_memberships_for_user.assert_called_once_with("user-1")
       
    2:     @patch("app.routers.projects.event_bus.publish")
    2:     @patch("app.routers.projects.record_activity")
    2:     @patch("app.routers.projects.get_conn")
    2:     @patch("app.routers.projects.fetch_one")
    2:     def test_create_project_reuses_existing_row_on_conflict(self, mock_fetch_one, mock_get_conn, mock_record_activity, mock_publish):
    1:         mock_fetch_one.return_value = None
    1:         project_row = {"id": "project-1", "tenant_id": "tenant-1", "key": "GRABBIT", "name": "Grabbit", "visibility": "EVERYONE"}
       
    1:         def resolver(query, params):
    2:             normalized = " ".join(query.split()).lower()
    2:             if normalized.startswith("insert into projects"):
    1:                 return None
    1:             if normalized.startswith("select id, tenant_id, name, key, description, visibility, status, issue_counter, created_by, created_at, updated_at from projects where tenant_id = %s and key = %s limit 1"):
    1:                 return project_row
                   return None
       
    1:         mock_get_conn.return_value = FakeConnManager(resolver)
       
    1:         response = asyncio.run(create_project(ProjectCreate(name="Grabbit"), current_user={"id": "user-1", "role": "OWNER", "tenant_id": "tenant-1"}))
       
    1:         self.assertEqual(response["project"]["id"], "project-1")
    1:         self.assertEqual(response["project"]["key"], "GRABBIT")
    1:         mock_record_activity.assert_called_once()
    1:         mock_publish.assert_awaited_once()
       
       
    1: if __name__ == "__main__":
           unittest.main()
